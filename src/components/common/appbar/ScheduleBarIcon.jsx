import AppBarIcon from "./AppBarIcon";

const ScheduleBarIcon = () => {
  return (
    <AppBarIcon iconName="StoreClock" title="Open Now" subtitle="Closes 5:00 PM" />
  );
};

export default ScheduleBarIcon;
