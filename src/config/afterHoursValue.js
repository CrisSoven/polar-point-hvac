/**
 * Defines the after-hours service rate multiplier.
 * This value is used to indicate the additional cost factor for services
 * provided outside regular business hours.
 */
const afterHours = "1.5";

export default afterHours;
